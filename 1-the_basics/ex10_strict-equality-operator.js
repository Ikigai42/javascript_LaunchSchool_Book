/*
10. What value does the following expression evaluate to?
'foo' === 'Foo'
*/

//Case matters when comparing strings so, the expression evaluates to the boolean value false.

console.log('foo' === 'Foo');
