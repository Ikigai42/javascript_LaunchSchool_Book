/*
8. Create an array named names that contains a list of pet names. For instance:

name
asta
butterscotch
neptune
darwin

*/

let names = ['asta', 'spot', 'neptune', 'darwin'];

let moreNames = ['mr robot', 'somebody', ...names];

console.log(moreNames);
