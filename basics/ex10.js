/*
10. What value does the following expression evaluate to?
'foo' === 'Foo'

*/

console.log(`The strict equality operator === checks for equality of the two operands, since case matters, 'foo' === 'Foo' returns the Boolean: false.`);